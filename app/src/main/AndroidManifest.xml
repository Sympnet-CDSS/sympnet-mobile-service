<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.SympNetMobile"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCUPDh57AstBm2EU1H0Raf2g6wUWqwgTHY" />

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SympNetMobile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.auth.OnboardingActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SympNetMobile" />

        <activity android:name=".activities.auth.LoginActivity" />
        <activity android:name=".activities.auth.RegisterActivity" />
        <activity android:name=".activities.auth.ForgotPasswordActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.profile.SettingsActivity" />
        <activity android:name=".activities.profile.EditProfileActivity" />
        <activity android:name=".activities.profile.PasswordManagerActivity" />
        <activity android:name=".activities.profile.NotificationSettingsActivity" />
        <activity android:name=".home.ActivityHome"/>
        <activity android:name=".activities.doctor.DoctorDetailsActivity" />
        <activity android:name=".activities.doctor.AllDoctorsActivity" />
        <activity android:name=".activities.appointment.BookAppointmentActivity" />
        <activity android:name=".activities.appointment.AppointmentDetailActivity" />
        <activity android:name=".activities.doctor.FavoriteDoctorsActivity" />
        <activity android:name=".activities.profile.HelpActivity" />
        <activity android:name=".activities.profile.PrivacyPolicyActivity" />
        <activity android:name=".activities.profile.LanguageActivity" />
        <activity android:name=".activities.notification.NotificationDetailsActivity" />

        <!-- Chat & Video Call -->
        <activity android:name=".activities.chat.ChatListActivity" />
        <activity android:name=".activities.chat.ChatDetailActivity" />


    </application>

</manifest>